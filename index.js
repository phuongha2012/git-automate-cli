const chalk = require('chalk'); // creates large letters with text
const figlet = require('figlet'); // applies colors and styles
const clear = require('clear'); // removes current output in the console

const files = require('./lib/files');


